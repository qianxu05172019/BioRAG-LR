# 核心依赖 - 使用旧版但稳定的版本
streamlit==1.20.0
langchain==0.0.154
openai==0.27.8

# 向量存储 - 使用旧版chromadb，避免依赖编译问题
chromadb==0.3.21

# 文档处理 - 移除tiktoken，使用旧版pypdf
pypdf3==1.0.6

# 通用依赖 - 指定旧版但稳定的版本
python-dotenv==0.21.0
pydantic==1.10.8