

# 核心依赖 - 使用更新的版本
streamlit>=1.20.0
langchain>=0.0.200
openai>=0.27.8

# 向量存储 - 使用更新的chromadb
chromadb>=0.3.21

# 文档处理 - 使用更通用的pypdf
pypdf>=3.0.0

# 通用依赖
python-dotenv>=0.21.0
pydantic>=1.10.8,<2.0.0